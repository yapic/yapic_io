# if you change this file you should build a new docker image (see e.g. ./debian.Dockerfile)
numpy>=1.13.1
munkres>=1.0.8
scikit_image>=0.12.3
pyilastik @ git+http://gitlab.dzne.de/idaf/pyilastik.git@master
bigtiff @ git+https://github.com/yapic/bigtiff.git@master

